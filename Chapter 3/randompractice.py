# import statement - -load modules (files with code)
# random module -  generate random numbers
import random

# randint - produce a random integer
print(random.randint(1, 10))

num = random.randint(1, 10)
print(num)

# randrange - produce a random integer (not inclusive; can include a step)
print(random.randrange(2, 11, 2))